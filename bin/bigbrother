#!/usr/bin/env bash
# Auto-generated by symlink-farm.sh — do not edit.
# Implementation: bin/lib/bigbrother/main.sh
LUSH_ROOT="${BASHRC:-$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")}"
MAIN="$LUSH_ROOT/bin/lib/bigbrother/main.sh"
[[ -r "$MAIN" ]] || "$LUSH_ROOT/bin/lush" install "bigbrother" || exit 1
exec bash "$MAIN" "$@"
