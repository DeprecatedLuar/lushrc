#!/usr/bin/env bash
# Auto-generated by symlink-farm.sh — do not edit.
# Implementation: bin/lib/yoink/main.sh
LUSH_ROOT="${BASHRC:-$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")}"
MAIN="$LUSH_ROOT/bin/lib/yoink/main.sh"
[[ -r "$MAIN" ]] || "$LUSH_ROOT/bin/lush" install "yoink" || exit 1
exec bash "$MAIN" "$@"
